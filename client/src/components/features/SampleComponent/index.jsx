import React from 'react';
import { DefaultLayout } from '../../layout';
import { Typography } from '@mui/material';

const SampleComponentDefault = () => {
  return (
    <DefaultLayout>
      <Typography variant="h5">Sample Component</Typography>
    </DefaultLayout>
  );
};

export default SampleComponentDefault;